using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class EnemyState : MonoBehaviour 
{
	/*
	NavMeshAgent navMeshAgent;

	public GameObject spriteObject;

	SpriteRenderer currentSprite;

	Animator animator;
	AnimatorStateInfo currentStateInfo;

	static int currentAnimState;
	static int attackState = Animator.StringToHash ("Base Layer.EnemyAttack");
	static int walkState = Animator.StringToHash ("Base Layer.EnemyWalk");

	void Awake()
	{
		navMeshAgent = GetComponent<NavMeshAgent> ();
		animator = spriteObject.GetComponent<Animator> ();
	}
	
	// Update is called once per frame
	void Update () 
	{
		currentStateInfo = animator.GetCurrentAnimatorStateInfo (0);
		currentAnimState = currentStateInfo.fullPathHash;
	}
	*/
}
